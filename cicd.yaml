apiVersion: apps/v1
kind: Deployment
metadata: 
  name: my-deployment
  labels:
    app: myapp

spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template: 
    metadata:
      name: tomcat-pod
      labels:
        app: myapp

    spec:
      containers:
        - name: tomcat
          image: giridaran/newapp:latest
          ports:
          - containerPort: 8085


---

apiVersion: v1
kind: Service
metadata: 
  name: tomcat-sv

spec:
  type: NodePort
  ports:
  - targetPort: 8085
    port: 8085
    nodePort: 30780
  selector:
    app: myapp
